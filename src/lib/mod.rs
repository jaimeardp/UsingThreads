pub mod models;
pub mod utils;

//pub use models::Person;
//pub use utils::write_json_to_file;

//use rand::Rng;

//use json_util::write_json_to_file;

/*fn gen_persons_vector(num_persons: u8) -> Vec<json_util::Person> {
    let mut persons = Vec::new();
    for _ in 0..num_persons {
        let name = String::from("Hello, ");
        let age = rand::thread_rng().gen_range(0..100);
        let person = json_util::Person { name, age };
        persons.push(person);
    }
    return persons;
}*/

/*pub fn mod_writ_json_to_file(persons: &Vec<json_util::Person>) {
    //let ctx_persons = gen_persons_vector(5);
    write_json_to_file(persons);
}*/
